# Node.js portable runtime (~80 MB, bundled separately)
runtime/

# SQLite WAL/SHM files (transient, recreated on startup)
data/*.db-shm
data/*.db-wal

# npm artifacts
server/package-lock.json
server/node_modules/.package-lock.json
